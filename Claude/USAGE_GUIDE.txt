"""
========================================
完整操作指南 - 本地部署修改
Complete Operation Guide - Local Deployment
========================================

作者: Claude (基于用户需求)
日期: 2025-12-06
版本: 2.0 - 历史数据驱动WarmStart

本指南详细说明如何在本地代码库中应用修改。
"""

# ============================================================================
# 📋 修改概览
# ============================================================================

"""
本次修改解决的问题:

✅ 问题1: WarmStart的prompt是硬编码的
   → 解决: 创建BatteryKnowledgePromptGenerator,基于领域知识动态生成高质量prompt

✅ 问题2: Result只保存最优解,格式不完整
   → 解决: 创建ResultManager,保存完整的评估数据库和详细统计

✅ 问题3: WarmStart无法利用历史数据
   → 解决: 创建HistoricalWarmStart,自动从历史Result中学习最优/最差/随机解

✅ 问题4: 缺乏高质量的电化学领域知识
   → 解决: 基于最新学术研究整合SEI生长、锂析出、热管理等物理约束
"""

# ============================================================================
# 📁 文件结构
# ============================================================================

"""
你需要添加的新文件:

BO/
├── llmbo_core/
│   ├── prompt_generator.py           ← 新增 (动态Prompt生成器)
│   ├── result_manager.py             ← 新增 (Result数据管理)
│   ├── historical_warmstart.py       ← 新增 (历史数据驱动WarmStart)
│   └── multi_objective_evaluator.py  ← 修改 (集成新WarmStart)
│
├── LLM_Enhanced_Multi-Objective_Bayesian_Optimization.py  ← 修改 (集成ResultManager)
│
└── results/                          ← 已存在 (保存目录,格式改进)

总共: 3个新文件 + 2个修改
"""

# ============================================================================
# 🚀 步骤1: 复制新文件到本地项目
# ============================================================================

"""
1.1 下载Claude生成的文件:
    - prompt_generator.py
    - result_manager.py  
    - historical_warmstart.py

1.2 放置位置:
    将以上3个文件复制到你的项目的 llmbo_core/ 目录下
    
    例如:
    /your/project/path/BO/llmbo_core/prompt_generator.py
    /your/project/path/BO/llmbo_core/result_manager.py
    /your/project/path/BO/llmbo_core/historical_warmstart.py

1.3 验证:
    cd /your/project/path/BO/llmbo_core/
    ls -l prompt_generator.py result_manager.py historical_warmstart.py
    
    应该看到3个文件都存在
"""

# ============================================================================
# 🔧 步骤2: 修改 multi_objective_evaluator.py
# ============================================================================

"""
2.1 打开文件:
    vim llmbo_core/multi_objective_evaluator.py
    # 或使用你喜欢的编辑器

2.2 在文件开头添加导入 (大约第10-20行):

    # ====== 原有导入 ======
    import numpy as np
    import asyncio
    from typing import Dict, List, Optional
    # ... 其他导入 ...
    
    # ====== 新增导入 ======
    from historical_warmstart import HistoricalWarmStart  # 新增这一行

2.3 找到 initialize_with_llm_warmstart 方法 (大约第300行左右)
    搜索: def initialize_with_llm_warmstart

2.4 替换整个方法体:

    async def initialize_with_llm_warmstart(
        self,
        n_strategies: int = 5,
        llm_api_key: Optional[str] = None,
        llm_base_url: str = 'https://api.nuwaapi.com/v1',
        llm_model: str = "gpt-3.5-turbo"
    ) -> List[Dict]:
        \"\"\"
        使用 LLM 进行热启动初始化 - 新版本 (历史数据驱动)
        \"\"\"
        
        if self.verbose:
            print("\n" + "=" * 70)
            print("开始 LLM Warm Start 初始化 (历史数据驱动版)")
            print("=" * 70)
            print(f"目标策略数量: {n_strategies}")
            print(f"API Key: {'已提供' if llm_api_key else '未提供（将使用随机策略）'}")
            print("=" * 70)
        
        results = []
        
        # ====== 使用新的HistoricalWarmStart ======
        if llm_api_key is not None:
            try:
                if self.verbose:
                    print("\n使用HistoricalWarmStart生成策略...")
                
                # 创建HistoricalWarmStart实例
                warmstart_generator = HistoricalWarmStart(
                    result_dir='./results',
                    llm_api_key=llm_api_key,
                    llm_base_url=llm_base_url,
                    llm_model=llm_model,
                    verbose=self.verbose
                )
                
                # 生成策略
                strategies = await warmstart_generator.generate_warmstart_strategies_async(
                    n_strategies=n_strategies,
                    n_historical_runs=5,
                    objective_weights=self.weights,
                    exploration_mode='balanced'
                )
                
                if self.verbose:
                    print(f"[OK] HistoricalWarmStart成功生成 {len(strategies)} 个策略")
                
            except Exception as e:
                if self.verbose:
                    print(f"⚠️  HistoricalWarmStart失败: {e}")
                    print("   回退到随机策略生成...")
                
                strategies = self._generate_random_strategies(n_strategies)
        else:
            if self.verbose:
                print("\n使用随机策略生成...")
            
            strategies = self._generate_random_strategies(n_strategies)
        
        # ====== 评估所有策略 (保持原有逻辑) ======
        if self.verbose:
            print(f"\n开始评估 {len(strategies)} 个策略...")
        
        for i, strategy in enumerate(strategies, 1):
            params = strategy['params']
            
            try:
                scalarized = self.evaluate(
                    current1=params['current1'],
                    charging_number=params['charging_number'],
                    current2=params['current2']
                )
                
                latest_log = self.detailed_logs[-1]
                
                result = {
                    'params': params,
                    'scalarized': scalarized,
                    'objectives': latest_log['objectives'],
                    'valid': latest_log['valid'],
                    'source': strategy.get('source', 'llm_warmstart'),
                    'reasoning': strategy.get('reasoning', '')
                }
                
                results.append(result)
                
                if self.verbose:
                    print(f"  策略 {i}/{len(strategies)}: "
                          f"I1={params['current1']:.2f}A, "
                          f"t1={params['charging_number']}, "
                          f"I2={params['current2']:.2f}A "
                          f"→ 标量化={scalarized:.4f}")
            
            except Exception as e:
                if self.verbose:
                    print(f"  ✗ 策略 {i} 评估失败: {e}")
                continue
        
        if self.verbose:
            print(f"\n[OK] Warm Start完成! 成功评估 {len(results)}/{len(strategies)} 个策略")
            print("=" * 70)
        
        return results

2.5 删除或注释掉原有的 _llm_generate_strategies 方法:

    搜索: async def _llm_generate_strategies
    
    将整个方法注释掉或删除:
    
    # ❌ 删除这个方法 (硬编码prompt)
    # async def _llm_generate_strategies(
    #     self,
    #     n_strategies: int,
    #     api_key: str,
    #     base_url: str,
    #     model: str
    # ) -> List[Dict]:
    #     ...原有代码...

2.6 保存文件:
    :wq (vim)
    或 Ctrl+S (其他编辑器)
"""

# ============================================================================
# 🔧 步骤3: 修改主优化器文件
# ============================================================================

"""
3.1 打开文件:
    vim LLM_Enhanced_Multi-Objective_Bayesian_Optimization.py

3.2 在文件开头添加导入 (大约第50-60行):

    # ====== 原有导入 ======
    from multi_objective_evaluator import MultiObjectiveEvaluator
    from LLM_enhanced_surrogate_modeling import LLMEnhancedBO
    from LLM_Enhanced_Expected_Improvement import LLMEnhancedEI
    
    # ====== 新增导入 ======
    from result_manager import ResultManager  # 新增这一行

3.3 在 __init__ 方法中初始化ResultManager (大约第100行):

    def __init__(
        self,
        llm_api_key: str,
        ...
        save_dir: str = './results'
    ):
        # ... 原有代码 ...
        
        # 创建保存目录
        os.makedirs(save_dir, exist_ok=True)
        
        # ====== 新增: 初始化ResultManager ======
        self.result_manager = ResultManager(save_dir=save_dir)  # 新增这一行
        
        # 优化历史
        self.optimization_history = []
        
        # ... 其余代码保持不变 ...

3.4 修改 export_results 方法 (大约第400-450行):

    def export_results(self, filename: str = None) -> str:
        \"\"\"
        导出结果到JSON文件 - 使用ResultManager保存完整数据
        \"\"\"
        
        if filename is None:
            timestamp = datetime.now().strftime("%Y%m%d_%H%M%S")
            run_id = f"llm_mobo_{timestamp}"
        else:
            run_id = filename.replace('.json', '')
        
        # 获取完整数据
        database = self.evaluator.export_database()
        best_solution = self.evaluator.get_best_solution()
        pareto_front = self.evaluator.get_pareto_front()
        statistics = self.evaluator.get_statistics()
        
        # 配置信息
        config = {
            'llm_model': self.llm_model,
            'n_warmstart': self.n_warmstart,
            'n_random_init': self.n_random_init,
            'n_iterations': self.n_iterations,
            'objective_weights': self.evaluator.weights,
            'enable_llm_warmstart': self.enable_llm_warmstart,
            'enable_llm_surrogate': self.enable_llm_surrogate,
            'enable_llm_ei': self.enable_llm_ei
        }
        
        # ====== 使用ResultManager保存 ======
        filepath = self.result_manager.save_optimization_run(
            run_id=run_id,
            database=database,
            best_solution=best_solution,
            pareto_front=pareto_front,
            config=config,
            statistics=statistics,
            metadata={
                'elapsed_time': self.optimization_history[-1].get('elapsed_time', 0) if self.optimization_history else 0
            }
        )
        
        print(f"\n✓ 完整优化结果已保存")
        return filepath

3.5 保存文件
"""

# ============================================================================
# ✅ 步骤4: 验证修改
# ============================================================================

"""
4.1 测试新模块是否能导入:

    cd /your/project/path/BO/llmbo_core/
    python3 -c "from prompt_generator import BatteryKnowledgePromptGenerator; print('✓ prompt_generator导入成功')"
    python3 -c "from result_manager import ResultManager; print('✓ result_manager导入成功')"
    python3 -c "from historical_warmstart import HistoricalWarmStart; print('✓ historical_warmstart导入成功')"

4.2 测试独立功能:

    # 测试Prompt生成器
    python3 prompt_generator.py
    
    # 测试Result管理器
    python3 result_manager.py
    
    # 测试HistoricalWarmStart
    python3 historical_warmstart.py

4.3 检查语法错误:

    python3 -m py_compile multi_objective_evaluator.py
    python3 -m py_compile LLM_Enhanced_Multi-Objective_Bayesian_Optimization.py

    如果没有输出,说明语法正确
"""

# ============================================================================
# 🚀 步骤5: 运行优化测试
# ============================================================================

"""
5.1 创建测试脚本 test_new_warmstart.py:

    import asyncio
    from LLM_Enhanced_Multi_Objective_Bayesian_Optimization import LLMEnhancedMultiObjectiveBO
    
    async def test():
        # 创建优化器
        optimizer = LLMEnhancedMultiObjectiveBO(
            llm_api_key='your-api-key-here',  # 替换为真实API key
            llm_base_url='https://api.nuwaapi.com/v1',
            llm_model='gpt-3.5-turbo',
            n_warmstart=5,
            n_iterations=10,  # 测试用少量迭代
            n_random_init=3,
            enable_llm_warmstart=True,
            verbose=True,
            save_dir='./results'
        )
        
        # 运行优化
        results = await optimizer.optimize_async()
        
        # 导出结果
        optimizer.export_results()
        
        print("\n✓ 测试完成!")
    
    if __name__ == "__main__":
        asyncio.run(test())

5.2 运行测试:

    python3 test_new_warmstart.py

5.3 检查输出:

    应该看到:
    - [HistoricalWarmStart] 初始化完成
    - 加载历史数据 (如果有的话)
    - 生成高质量prompt
    - LLM生成策略
    - 评估策略
    - 保存完整结果到 results/ 目录

5.4 验证保存的结果文件:

    cat results/llm_mobo_YYYYMMDD_HHMMSS.json | head -n 50
    
    应该看到完整的JSON结构,包括:
    - run_id
    - timestamp
    - config
    - statistics
    - best_solution
    - pareto_front
    - database (所有评估点!)  ← 关键!
    - analysis
"""

# ============================================================================
# 📊 步骤6: 验证历史数据功能
# ============================================================================

"""
6.1 运行第二次优化:

    第二次运行时,HistoricalWarmStart会自动:
    - 加载第一次运行的结果
    - 提取最优10个、最差10个解
    - 在prompt中提供few-shot examples
    - 生成改进的策略

6.2 查看prompt内容 (调试用):

    在 historical_warmstart.py 的 _call_llm_async 方法中添加:
    
    print("\n生成的Prompt:")
    print("=" * 80)
    print(prompt)
    print("=" * 80)

6.3 比较两次运行的结果:

    第一次: 纯领域知识 + 随机探索
    第二次: 领域知识 + 历史最优解引导

    第二次应该收敛更快!
"""

# ============================================================================
# 🎯 步骤7: 高级配置 (可选)
# ============================================================================

"""
7.1 调整探索模式:

    在调用时设置exploration_mode:
    
    - 'conservative': 保守,靠近已知好解
    - 'balanced': 平衡,混合已知和探索 (默认)
    - 'aggressive': 激进,探索边界区域

    warmstart_generator.generate_warmstart_strategies_async(
        n_strategies=5,
        exploration_mode='aggressive'  # 修改这里
    )

7.2 调整历史数据加载量:

    n_historical_runs=10  # 加载最近10次运行 (默认5)

7.3 调整Few-Shot样例数量:

    在 prompt_generator.py 的 generate_warmstart_prompt 方法中:
    
    historical_best=historical_best[:5]  # 改为5个最优解
    historical_worst=historical_worst[:3]  # 改为3个最差解

7.4 自定义领域知识:

    在 prompt_generator.py 的 __init__ 中修改 self.physics_knowledge
    添加你自己的电化学见解!
"""

# ============================================================================
# 🐛 故障排查
# ============================================================================

"""
问题1: ImportError: No module named 'historical_warmstart'
解决: 确保文件放在正确的目录,并且目录中有 __init__.py

问题2: LLM返回的不是有效JSON
解决: 检查LLM模型是否支持JSON格式,或在prompt中强调JSON格式

问题3: 历史数据加载失败
解决: 检查 results/ 目录是否存在,是否有.json文件

问题4: 策略生成数量不足
解决: LLM可能生成无效策略,检查参数验证逻辑

问题5: 评估失败
解决: 检查SPM模型是否正常工作,参数是否在有效范围内
"""

# ============================================================================
# ✅ 验证清单
# ============================================================================

"""
完成后检查:

[ ] 3个新文件已复制到 llmbo_core/ 目录
[ ] multi_objective_evaluator.py 已修改
    [ ] 添加了 HistoricalWarmStart 导入
    [ ] 修改了 initialize_with_llm_warmstart 方法
    [ ] 删除了 _llm_generate_strategies 方法
[ ] LLM_Enhanced_Multi-Objective_Bayesian_Optimization.py 已修改
    [ ] 添加了 ResultManager 导入
    [ ] 初始化了 self.result_manager
    [ ] 修改了 export_results 方法
[ ] 所有模块可以成功导入
[ ] 测试运行成功
[ ] 结果文件包含完整database
[ ] 第二次运行可以加载历史数据
[ ] Prompt包含few-shot examples

全部勾选 → 修改完成! 🎉
"""

# ============================================================================
# 📚 进一步优化建议
# ============================================================================

"""
1. 集成到Comparison框架:
   - 修改 comparison_runner.py 使用 ResultManager
   - 比较不同算法时也保存完整database

2. 可视化改进:
   - 使用 analysis 字段生成更详细的图表
   - 对比历史运行的收敛曲线

3. 自动调优:
   - 根据历史性能自动调整 exploration_mode
   - 检测停滞时切换到 aggressive

4. 增量学习:
   - 每N次运行更新prompt template
   - 从失败策略中学习避免区域

5. 多目标Pareto学习:
   - 不仅学习标量最优,还学习Pareto前沿
   - 生成多样化的Pareto解

参考论文中的方法继续扩展!
"""

# ============================================================================
# 📧 需要帮助?
# ============================================================================

"""
如果遇到问题:

1. 检查本文档的故障排查部分
2. 运行独立测试脚本验证各模块
3. 查看MODIFICATION_EXAMPLE.py中的代码示例
4. 参考论文 manuscript1.pdf 的 Figure 2-4

祝优化顺利! 🚀
"""
